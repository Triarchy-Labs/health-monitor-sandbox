name: Dummy Failing Plugin

on:
  workflow_dispatch:

jobs:
  fail:
    name: Execute Plugin Logic
    runs-on: ubuntu-latest
    steps:
      - name: Setup environment
        run: echo "Initializing plugin..."

      - name: Execute core logic
        run: |
          echo "Simulating a plugin crash..."
          echo "ERROR: Missing configuration or API rate limit exceeded."
          exit 1
